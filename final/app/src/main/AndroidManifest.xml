<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nimisha.afinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Splash_Screen" />
        <activity android:name=".Landing_Page" />
        <activity android:name=".Library_srv" />
        <activity android:name=".Srv_of_cspace" />
        <activity android:name=".cspace_test" />
        <activity android:name=".customGrid" />
        <activity android:name=".PrintandScan" />
        <activity android:name=".test"></activity>
    </application>

</manifest>