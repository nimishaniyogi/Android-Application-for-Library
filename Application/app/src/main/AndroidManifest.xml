<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wsu.library_application">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LandingPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".popular_links" />
        <activity android:name=".Resources" />
        <activity android:name=".electronic_book" />
        <activity android:name=".more_resources" />
        <activity android:name=".journals" />
        <activity android:name=".feedback" />
        <activity android:name=".cspace" />
        <activity android:name=".cspace_services"></activity>
    </application>

</manifest>